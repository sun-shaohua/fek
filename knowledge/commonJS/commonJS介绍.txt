commonJs最初服务于服务端，它的载体是前端语言 JavaScript，为后面前端模块化的盛行产生了深远的影响，奠定了结实的基础。

node应用有模块组成，采用commonJS模块规范，每一个文件是一个模块，拥有自己对立的作用域，变量，以及方法等，对其他模块是不可见的。
commonJS规定每个模块内部，用module变量代表当前模块，导出这个模块用exports(module.exports)是对外的接口，引用使用require方法加载这个模块。

commonJS模块的特点
1.所有的代码都运行在模块作用域内，不会造成全局的污染。
